# --- ETAPA 1: CONSTRUCCIÓN (MAVEN + JAVA 21) ---
# Usamos una imagen específica de Maven con Java 21 (Amazon Corretto)
FROM maven:3.9.6-amazoncorretto-21 AS build
WORKDIR /app
COPY pom.xml .
COPY src ./src

# EL COMANDO MÁGICO:
# -Dmaven.test.skip=true : NO compila los tests (evita errores de código de test)
# -DskipTests : A veces no es suficiente, por eso usamos el de arriba.
RUN mvn clean package -Dmaven.test.skip=true

# --- ETAPA 2: EJECUCIÓN (JAVA 21) ---
FROM amazoncorretto:21
WORKDIR /app
# Copiamos el .jar generado
COPY --from=build /app/target/*.jar app.jar

# Tu aplicación usa el puerto 9090
EXPOSE 9090
ENTRYPOINT ["java", "-jar", "app.jar"]
